import { getDocs,collection,query,where } from "@firebase/firestore"
import { db } from "../config/@firebase"
export async function doc_exists(collection,_query)
{
    try{
        // const
    }
    catch(e){

    }
}